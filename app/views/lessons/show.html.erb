<br />

<div class="booyah-box col-10 offset-1">
  <h1><%= current_lesson.title %></h1>
  <p>
    <%= current_lesson.subtitle %>
  </p>
</div>

<div class="clr"></div>
<br />
<div class="booyah-box col-10 offset-1">
  <%= simple_form_for @photo, url: instructor_section_lessons_path(current_lesson) do |f| %>
    <%= f.input :image %>
    <%= f.submit "Upload", class: 'btn btn-primary' %>
  <% end %>
</div>

